<Window x:Class="WPF.HotelBooking.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.HotelBooking.UI"
        xmlns:view="clr-namespace:WPF.HotelBooking.UI.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!--<view:NavigationView DataContext="{Binding NavigationViewModel}"/>-->
        <!--<view:RoomDetailView Grid.Column="1" DataContext="{Binding RoomDetailViewModel}"/>-->
        <!--<view:UserDetailView Grid.Column="1" DataContext="{Binding UserDetailViewModel}"/>-->
        <!--<view:RoomDetailView Grid.Column="1" DataContext="{Binding RoomDetailViewModel}"/>-->

        <Frame Name="Column1" Grid.Column="1" Grid.ColumnSpan="2"/>
        <!--<Frame Name="Column2" Grid.Column="2"/>-->
        <Frame Name="Column3" Grid.Column="3"/>


    </Grid>
</Window>
